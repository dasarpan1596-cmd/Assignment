#include<stdio.h>  //find floor and ceil value
int main()
{
    float a=12.5,b=-10.3;
    printf("\nFloor(+ve):%f     ceil(+ve):%f",floor(a),ceil(a));
    printf("\nFloor(-ve):%f     ceil(-ve):%f",floor(b),ceil(b));
}